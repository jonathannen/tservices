# Ticker

Supports processes that run continuously (a "tick" is an execution unit). This is for processes that run in a general `while(true) { .. }` event loop.

Note that TickerServices are _also_ RunstateServices.
